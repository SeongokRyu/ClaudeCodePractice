[project]
name = "practice-10-bug-debugging"
version = "0.1.0"
description = "Practice 10: 버그 디버깅"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["src"]
asyncio_mode = "auto"

[dependency-groups]
dev = ["pytest", "pytest-asyncio"]
