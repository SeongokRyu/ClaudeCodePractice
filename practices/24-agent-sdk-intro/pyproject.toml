[project]
name = "practice-24-agent-sdk-intro"
version = "0.1.0"
description = "Practice 24: Agent SDK 입문"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.1.0",
    "anthropic>=0.40.0",
]
