[project]
name = "practice-34-monitoring-logging"
version = "0.1.0"
description = "Practice 34: Agent Monitoring and Logging"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["src/logging"]

[dependency-groups]
dev = ["pytest", "pytest-cov"]
