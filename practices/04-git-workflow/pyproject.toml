[project]
name = "practice-04-git-workflow"
version = "0.1.0"
description = "Practice 04: Git Workflow"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["src"]

[dependency-groups]
dev = ["pytest"]
